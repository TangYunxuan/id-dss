############################################
# ID-DSS Frontend Environment (Vite)
#
# For local overrides, copy to `frontend/.env.local` (not committed),
# or set these as build-time environment variables in your GitHub Pages deployment.
#
# For GitHub Pages production builds, you can copy:
#   `frontend/env.production.example` -> `frontend/.env.production`
# (the `.env.production` file should NOT be committed).
############################################

# Backend API base URL (must include /api/v1)
# - Local dev: leave unset (Vite proxy handles /api -> localhost:8000)
# - Production: set to your Render/custom API domain
#
# Example (Render default domain):
# VITE_API_BASE_URL=https://your-service.onrender.com/api/v1
#
# Example (recommended custom subdomain):
# VITE_API_BASE_URL=https://api.tangyunxaun.com/api/v1

# Optional: public base path for path-based deployments
# Example: deploy app at https://tangyunxuan.com/id-dss/
# VITE_PUBLIC_BASE_PATH=/id-dss/

